kubectl create secret docker-registry --dry-run=client $secret_name \
  --docker-server=hub.docker.com \
  --docker-username=hoangbau38 \
  --docker-password=1q2w3e4r%T -o yaml > docker-secret.yaml